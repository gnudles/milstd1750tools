<<<<<<< SEARCH
  f_argv[0] = (char *) calloc (1, 256);
  for (i = 1; i <= MAXCOMARGS; i++)
    if ((f_argv[i] = (char *) calloc (1, 64)) == NULL)
      problem ("interpreter: could not allocate cmd string space");

  while (1)
=======
  while (1)
>>>>>>> REPLACE
<<<<<<< SEARCH
      if (have_args)
	*p = ' ';
      strncpy (f_argv[0], keyword, 255);
      f_argv[0][255] = '\0';
      f_argc = 1;
      if (have_args)
	{
	  while (*p)
	    {
	      while (isspace (*p))
		p++;
	      if (*p == '\0')
		break;
	      i = 0;
	      while (*p && ! isspace (*p) && i < 63)
		  *(f_argv[f_argc] + i++) = *p++;
	      while (*p && ! isspace (*p))
		  p++; /* skip rest of arg */
	      *(f_argv[f_argc++] + i) = '\0';
	      if (f_argc >= MAXCOMARGS) break;
	    }
	}
      *f_argv[f_argc] = '\0';
=======
      if (have_args)
	*p = ' ';

      f_argv[0] = keyword;
      f_argc = 1;
      if (have_args)
	{
	  while (*p)
	    {
	      while (isspace (*p))
		p++;
	      if (*p == '\0')
		break;
	      f_argv[f_argc++] = p;
	      while (*p && ! isspace (*p))
		p++;
	      if (*p != '\0')
	        {
	          *p++ = '\0';
	        }
	      if (f_argc >= MAXCOMARGS) break;
	    }
	}
      f_argv[f_argc] = NULL;
>>>>>>> REPLACE
